<%= form_for([@post, @post.comments.build]) do |f| %>
	<%= f.label :name, "Тема" %><br>
	<%= f.text_field :name %><br>
	<br>
	<%= f.label :body, "Комментарий" %><br>
	<%= f.text_area :body %><br>
	<br>
	<%= f.submit "Добавить"%>
<% end %>